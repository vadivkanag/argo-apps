apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "custdatasvc.fullname" . }}-test-connection"
  labels:
    {{- include "custdatasvc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "custdatasvc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
